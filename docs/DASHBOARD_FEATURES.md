# 📊 Новый дашборд "Сегодня"

## ✨ Что добавлено

### 1. **Мотивационная статистика** 🎉
- Случайная мотивационная цитата
- 3 ключевые метрики:
  - 🔥 Лучшая серия (longest streak)
  - ✅ Всего выполнено целей
  - 📊 Процент завершения за неделю
- Красивый градиентный фон

### 2. **Прогресс до следующего уровня** 🎮
- Визуальный прогресс-бар с градиентом
- Текущий уровень и XP
- Сколько XP осталось до следующего уровня
- Формула: уровень N требует N × 100 XP

### 3. **Круговые диаграммы прогресса** ⭕
Три круговые диаграммы:
- **Цели выполнены**: сколько целей выполнено на 100%
- **Активность сегодня**: сколько целей имеют запись за сегодня
- **Серии активны**: сколько целей имеют активную серию

Цвета:
- 🟢 Зелёный: ≥80%
- 🟡 Жёлтый: ≥50%
- 🔴 Красный: <50%

### 4. **Тепловая карта активности** 📅
- Последние 7 дней
- Показывает количество выполненных целей каждый день
- Интенсивность цвета = количество выполненных целей
- Сегодняшний день выделен синей рамкой
- Легенда внизу

### 5. **Улучшенный список целей** 🎯
- Заголовок с счётчиком выполненных
- Подсказка для новых пользователей
- Красивая пустая заглушка с кнопкой "Создать первую цель"

---

## 🎨 Компоненты

### `DailyProgressCircle.tsx`
Круговая диаграмма прогресса с анимацией:
- Props: `completed`, `total`, `label`
- SVG круг с stroke-dasharray анимацией
- Адаптивные цвета

### `LevelProgressBar.tsx`
Прогресс-бар уровня с градиентом:
- Props: `currentLevel`, `currentXP`, `xpForNextLevel`
- Градиентный фон (blue → purple)
- Градиентный прогресс-бар

### `WeeklyActivityHeatmap.tsx`
Тепловая карта активности:
- Props: `activityData` (массив {date, count})
- 7 дней с интенсивностью цвета
- Tooltip с деталями
- Легенда

### `MotivationalStats.tsx`
Мотивационная статистика:
- Props: `longestStreak`, `totalCompleted`, `weekCompletion`
- Случайная цитата из массива
- 3 статистики в карточках
- Градиентный фон

---

## 📊 Метрики и расчёты

### Активность за неделю
```typescript
// Группировка записей по датам
const activityByDate = new Map<string, number>();

// Для каждой записи:
const isComplete =
  entry.goal.type === "BINARY"
    ? entry.value === 1
    : entry.goal.target
      ? entry.value >= entry.goal.target
      : entry.value > 0;
```

### Процент завершения за неделю
```typescript
const totalGoalsThisWeek = summaries.length * 7;
const completedThisWeek = sum(activityByDate.values());
const weekCompletion = (completedThisWeek / totalGoalsThisWeek) * 100;
```

### Всего выполненных целей
```typescript
const totalCompletedAllTime = await prisma.goalEntry.count({
  where: {
    goal: { userId: user.id },
    OR: [
      { value: { gte: 1 }, goal: { type: "BINARY" } },
      { value: { gt: 0 }, goal: { type: { not: "BINARY" } } },
    ],
  },
});
```

---

## 🎯 Мотивационные цитаты

Случайная цитата при каждой загрузке:
- "Маленькие шаги каждый день — путь к большим целям! 🚀"
- "Вы на правильном пути! Продолжайте в том же духе! 💪"
- "Каждое выполненное действие приближает вас к мечте! ✨"
- "Прогресс — это не скорость, а направление! 🎯"
- "Сегодня лучший день, чтобы стать лучше! 🌟"

---

## 🌈 Дизайн

### Градиенты
- **Уровень**: `from-blue-50 to-purple-50` (light), `from-blue-950/30 to-purple-950/30` (dark)
- **Статистика**: `from-purple-50 via-pink-50 to-orange-50`
- **Прогресс-бар**: `from-blue-500 to-purple-500`

### Цветовая схема
- **Успех**: emerald-500/600
- **Предупреждение**: amber-500/600
- **Ошибка**: rose-500/600
- **Информация**: blue-500/600

### Анимации
- Круговые диаграммы: `transition-all duration-500`
- Прогресс-бар: `transition-all duration-500`
- Тепловая карта: `transition-colors`

---

## 🚀 Результат

**До:**
- Простой список целей
- 3 статистики вверху
- Минимум визуализаций

**После:**
- 🎨 Мотивирующий дашборд
- 📊 5+ визуализаций
- 🎯 Прогресс в реальном времени
- 🔥 Тепловая карта активности
- ✨ Мотивационные цитаты
- 🎮 Прогресс уровня

---

## 📱 Адаптивность

Все компоненты адаптивны:
- Круговые диаграммы: `grid-cols-3` (могут быть `grid-cols-1 md:grid-cols-3`)
- Тепловая карта: `flex` с равномерным распределением
- Статистика: `grid-cols-3`

---

**Готово! Дашборд теперь действительно мотивирует!** 🎉
